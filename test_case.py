class TestCase:
        def __init__(self, id, number, description, result):
                self.id = id
                self.number = number
                self.description = description
                self.result = result
